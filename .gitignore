node_modules/
dist/
.DS_Store
*.log
*.tsbuildinfo

.nuxt/
.output/
_temp_test/

# Other AI-tool local configs (per-machine, not project-shared)
.roo/
.kilo/
.qwen/
.venv/
.mempalace/
.tmp-mempalace/
.aider*

.nuxt
.output

# OpenCode config contains absolute paths regenerated by `cce init`
opencode.json

# Test artifacts
test-results.json
test.html

# Cursor IDE: only user-local files are ignored.
# Project-shared .cursor/rules, .cursor/commands, .cursor/skills,
# .cursor/plans and .cursor/constraints.md stay tracked.
.cursor/settings.json
.cursor/settings.local.json
.cursor/cache/
.cursor/plans/

# Claude Code: settings.json is regenerated by `cce init` with absolute paths.
# Only project-shared guidance (CLAUDE.md) stays tracked.
.claude/settings.json
.claude/settings.local.json

# Spec Kit work items
specs/

# CCE (code-context-engine)
# CCE local cache (per-machine, not for version control)
.cce/
# .mcp.json contains absolute paths regenerated by `cce init`
.mcp.json

# Rules audit output (transient log, regenerated on demand)
rules_audit_output.txt

# External tool installers / one-off artifacts
artifact/opencode-polza.sh
