# Game Life — Claude / agent entry

Thin project pointer. Coding standards live in `.cursor/rules/` and `.specify/memory/constitution.md`.

- Cursor + Speckit: use project Speckit skills/commands for work-items.
- Codex: global Honey / `~/.codex/AGENTS.md` owns lean style; keep this file short.
- Durable memory: `.specify/memory/` and `doc/`.
- If CCE MCP tools are available (`context_search`, `session_recall`, …), prefer them for exploration/recall; otherwise Grep/Read.

Be concise. Keep code, paths, commands, and errors exact.
