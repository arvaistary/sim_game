# Game Life — Claude / agent entry

Thin project pointer. The portable workflow lives in `agent-workflow/`; Game Life adapters live in `doc/agent-workflow/`.

- Codex: use `AGENTS.md` and `agent-workflow/`; global Honey owns lean style.
- Shared code rules: `.cursor/rules/10-typing.mdc`, `15-nuxt-typescript.mdc`, `20-code-style.mdc`, `30-architecture.mdc`, and `40-styles.mdc`.
- Durable project knowledge: `doc/`, `doc/decisions/`, and `doc/audit/`.
- If CCE MCP tools are available (`context_search`, `session_recall`, …), prefer them for exploration/recall; otherwise Grep/Read.

Be concise. Keep code, paths, commands, and errors exact.
