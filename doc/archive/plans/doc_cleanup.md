# План реструктуризации проектной документации

> Дата создания: 2026-06-02
> Статус: ⏳ К выполнению
> Цель: Привести документацию в актуальное состояние, отражающее реальный стек и архитектуру проекта (Nuxt 4 + Vue 3 + Pinia + TypeScript)

---

## Сводка

| Модуль | Задача | Приоритет | Статус |
|--------|--------|:---------:|:------:|
| M1 | Актуализация корневого README.md | P0 | ⏳ |
| M2 | Консолидация структуры директорий | P0 | ⏳ |
| M3 | Обновление doc/README.md (точка входа) | P0 | ⏳ |
| M4 | Архивация устаревших документов | P1 | ⏳ |
| M5 | Актуализация Architecture Overview | P1 | ⏳ |
| M6 | Создание ADR-журнала | P2 | ⏳ |
| M7 | Консолидация Roadmap | P1 | ⏳ |
| M8 | Актуализация API Reference | P2 | ⏳ |

---

## M1. Актуализация корневого README.md

**Приоритет:** P0 — первое, что видит любой заходящий в репозиторий

**Проблема:** README.md описывает Phaser 3, `src/bootstrap.js`, `ui-kit.js`, `src/scenes/` — это всё устарело. Проект уже на Nuxt 4 + Vue 3 + Pinia.

**Действия:**
1. Переписать раздел Tech Stack: Nuxt 4, Vue 3, Pinia, TypeScript, Vitest
2. Переписать раздел Current Project Structure: отразить `src/domain/`, `src/application/`, `src/stores/`, `src/composables/`, `src/components/`, `src/pages/`
3. Обновить инструкции запуска (nuxt dev, а не Phaser)
4. Удалить список «Implemented Scenes» — заменить на описание страниц/модулей
5. Обновить ссылки на документацию (убрать мёртвые ссылки на `doc/ecs/`)
6. Убрать упоминания `ui-kit.js`, `src/style.css`, `game-state.js`

**Критерий готовности:** Новый разработчик может прочитать README и понять, что это Nuxt-приложение, как его запустить и где искать код.

---

## M2. Консолидация структуры директорий

**Приоритет:** P0 — без этого остальные модули бессмысленны

**Проблема:** Документация разбросана по 4 папкам (`doc/`, `docs/`, `plans/`, `new_doc/`) без чёткой логики разделения.

**Целевая структура:**

```
doc/
├── README.md                          # Точка входа (M3)
├── core/                              # Основная документация
│   ├── ARCHITECTURE_OVERVIEW.md       # Обзор архитектуры (M5)
│   ├── IMPLEMENTATION_STATUS.md       # Статус реализации
│   ├── START_GAME_DOCUMENTATION.md    # Документация старта
│   ├── PAGES_REFERENCE.md             # Справочник страниц
│   └── ROADMAP.md                     # Консолидированный roadmap (M7)
│
├── adr/                               # Architecture Decision Records (M6)
│   ├── README.md                      # Индекс ADR
│   ├── 0001-phaser-to-nuxt-migration.md
│   ├── 0002-ecs-removal.md
│   ├── 0003-layered-architecture.md
│   └── ...
│
├── gdd/                               # Game Design Document (сохранить)
│   ├── README.md
│   ├── GDD.md
│   └── modules/
│
├── reference/                         # API Reference (M8)
│   ├── COMPOSABLES_REFERENCE.md
│   └── STORES_REFERENCE.md
│
├── archive/                           # Архив (M4)
│   ├── ecs/                           # ECS-related
│   ├── phaser-architecture/           # Phaser-related
│   ├── migration-plans/               # Выполненные планы миграций
│   └── refresh-plans/                 # Выполненные refresh-планы
│
└── guides/                            # Практические руководства
    ├── DESIGN_SYSTEM.md
    ├── MODAL_SYSTEM_GUIDE.md
    └── RULES_NUXT_ADAPTATION.md
```

**Действия:**
1. Переместить содержимое `docs/` в соответствующие папки `doc/`
2. Переместить `plans/*.md` в `doc/plans/` (или `doc/archive/migration-plans/` если выполнены)
3. Переместить `new_doc/` в `doc/guides/` или `doc/core/` по смыслу
4. Реорганизовать `doc/engine/` → `doc/archive/ecs/`
5. Удалить пустые директории после перемещения

**Критерий готовности:** Существуют только `doc/` и `plans/` (для активных планов). Нет дублирующихся папок.

---

## M3. Обновление doc/README.md

**Приоритет:** P0 — навигационный хаб

**Проблема:** Текущий `doc/README.md` упоминает ECS как актуальный слой, ссылается на `doc/engine/`, содержит устаревшие описания ролей.

**Действия:**
1. Обновить «Быстрый старт» — убрать шаг про ECS, добавить шаг про domain/application слои
2. Обновить структуру документации — отразить новую целевую структуру из M2
3. Обновить «Что где искать» — убрать ECS-ссылки, добавить ссылки на ADR
4. Обновить «Роли и документация» — убрать/archived разделы
5. Обновить глоссарий — убрать Legacy/Phaser термины, добавить domain/application
6. Обновить дату и версию

**Критерий готовности:** Все ссылки в README ведут на существующие файлы. Нет упоминаний Phaser как активного стека.

---

## M4. Архивация устаревших документов

**Приоритет:** P1 — снижение шума

**Проблема:** ~30 файлов в `doc/archive/` без пометок о статусе выполнения. Файлы `doc/engine/` описывают ECS, который уже удалён.

**Действия:**
1. Пройти по всем файлам в `doc/archive/` и добавить в начало статус-блок:
   ```markdown
   > **Статус:** ✅ Выполнено / ❌ Отменено / ⏸ Заморожено
   > **Дата выполнения:** YYYY-MM-DD
   > **Примечание:** Краткое резюме результата
   ```
2. Файлы `doc/engine/*.md` → переместить в `doc/archive/ecs/`
3. Файлы `doc/archive/phaser-architecture/` — оставить, пометить как архив
4. Файлы с суффиксом `-plan.md` в архиве — пометить статус выполнения
5. Удалить файлы, которые полностью дублируют другие (если найдутся)

**Критерий готовности:** Каждый файл в `archive/` имеет пометку статуса. Нет файлов без статуса.

---

## M5. Актуализация Architecture Overview

**Приоритет:** P1 — ключевой технический документ

**Проблема:** `doc/core/ARCHITECTURE_OVERVIEW.md` и `docs/ARCHITECTURE_CONTRACT.md` могут не соответствовать текущей архитектуре из `.cursor/rules/30-architecture.mdc`.

**Действия:**
1. Прочитать `.cursor/rules/30-architecture.mdc` как source of truth
2. Обновить `doc/core/ARCHITECTURE_OVERVIEW.md`:
   - Диаграмма слоёв: utils/constants → domain → application → infrastructure → stores/composables → components → pages
   - Правила импортов между слоями
   - Соглашения по компонентам (Ui*, layout/, game/)
   - Структура проекта (актуальная)
3. Объединить `docs/ARCHITECTURE_CONTRACT.md` с `ARCHITECTURE_OVERVIEW.md` (или удалить если дублирует)
4. Удалить `docs/architecture-research-report.md` если информация перенесена

**Критерий готовности:** Architecture Overview полностью соответствует правилам из `.cursor/rules/30-architecture.mdc`.

---

## M6. Создание ADR-журнала

**Приоритет:** P2 — инвестиция в будущее

**Проблема:** Архитектурные решения разбросаны по множеству файлов без единого индекса.

**Действия:**
1. Создать `doc/adr/README.md` с индексом всех ADR
2. Извлечь решения из существующих документов в ADR-формат:
   - `ARCHITECTURE_DECISION_HENDERSON_ADAPTATION.md` → ADR
   - `docs/decision-guide.md` → ADR
   - `docs/architecture-research-report.md` → ADR
   - Решение о миграции Phaser → Nuxt → ADR
   - Решение об удалении ECS → ADR
   - Решение о domain/application слоях → ADR
3. Формат каждого ADR:
   ```markdown
   # ADR-NNNN: [Название решения]

   **Дата:** YYYY-MM-DD
   **Статус:** Принято / Заменено ADR-XXXX

   ## Контекст
   ## Решение
   ## Последствия
   ```

**Критерий готовности:** Все ключевые архитектурные решения описаны в ADR. Есть индексный файл.

---

## M7. Консолидация Roadmap

**Приоритет:** P1 — планирование

**Проблема:** Планы разбросаны:
- `doc/ROADMAP.md`
- `docs/планы на будущее.md`
- `docs/планы по доработке.md`
- `doc/GDD/modules/13_roadmap.md`
- `doc/core/IMPLEMENTATION_STATUS.md` (частично дублирует)

**Действия:**
1. Прочитать все источники roadmap'а
2. Создать единый `doc/core/ROADMAP.md`:
   - Текущий статус (что реализовано)
   - Краткосрочные планы (1-2 спринта)
   - Среднесрочные планы (1-3 месяца)
   - Долгосрочные планы (backlog)
3. Переместить `docs/планы на будущее.md` и `docs/планы по доработке.md` в `doc/archive/`
4. Обновить ссылки в `doc/README.md`

**Критерий готовности:** Один файл ROADMAP.md — единственный источник правды о планах. Остальные файлы в архиве.

---

## M8. Актуализация API Reference

**Приоритет:** P2 — справочный материал

**Проблема:** `doc/COMPOSABLES_REFERENCE.md` существует, но может быть неполным. Нет единого справочника по stores.

**Действия:**
1. Обновить `doc/COMPOSABLES_REFERENCE.md`:
   - Перечислить все composables из `src/composables/`
   - Для каждого: сигнатура, параметры, возвращаемое значение
2. Создать `doc/reference/STORES_REFERENCE.md`:
   - Перечислить все stores из `src/stores/`
   - Для каждого: state, getters, actions
3. Переместить оба файла в `doc/reference/`

**Критерий готовности:** Каждый composable и store описан в reference с актуальной сигнатурой.

---

## Порядок выполнения

Зависимости между модулями:

```
M1 (README) ─────────────────────────────────────┐
M2 (Структура директорий) ──┬── M3 (doc/README)  │
                             ├── M4 (Архивация)   │
                             ├── M5 (Architecture) │
                             ├── M6 (ADR)         │
                             ├── M7 (Roadmap)      │
                             └── M8 (API Ref)     │
                                                   │
Рекомендуемый порядок:                             │
M1 → M2 → M3 → M4 → M5 → M7 → M6 → M8 ──────────┘
```

**M1 и M2** можно делать параллельно — они не зависят друг от друга.
**M3** зависит от M2 (нужно знать итоговую структуру).
**M4–M8** зависят от M2 (файлы должны быть на своих местах).
**M5, M7** можно делать параллельно после M2+M4.
**M6, M8** — независимы друг от друга, но зависят от M2.

---

## Итого

- **Файлов к обработке:** ~110
- **Новых файлов:** ~5-8 (ADR, stores reference)
- **Файлов к удалению/перемещению:** ~15-20
- **Ожидаемый результат:** Актуальная, структурированная документация, отражающая Nuxt 4 + Vue 3 + TypeScript стек с единой точкой входа

---

> **Последнее обновление плана:** 2026-06-02
