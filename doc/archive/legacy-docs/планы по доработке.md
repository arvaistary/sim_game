Что НЕ было сделано или сделано неполно в ходе последнего рефакторинга:
1. actions-store стал zombie

trackExecution не вызывается никем. Store делает только reset() и хранит lastExecutedAction / actionResults, которые никто не читает. Это мёртвый store — его можно либо:

подключить к useActions.executeAction чтобы реально трекать историю действий
либо удалить полностью, перенеся reset() в game-store напрямую
2. CareerTrackEntry дублируется в двух местах

Определён и в src/stores/game-store/index.types.ts (строка 55-65), и в src/application/game/index.types.ts (строка 132-142). Причём в game-store поля current, unlocked, missingProfessionalism, educationRequiredLabel — optional (?), а в application — required. Это не одно и то же. При этом game-store.getCareerTrack() возвращает application-тип, который совместим с store-типом по structural typing, но семантически это дублирование.

3. FinanceSnapshot в game-store тоже orphan

Определён в stores/game-store/index.types.ts (строка 75-77), но нигде не импортируется. В game-store нет метода, который бы его возвращал (я удалил getFinanceSnapshot).

4. getStats — метод без потребителей

game-store.getStats() (строка 247) возвращает StatsShortSnapshot, но grep показывает, что никто его не вызывает. Он дублирует computed<StatsSnapshot> выше.

5. getEducationRequirementLabel — фасад без потребителей в UI

game-store делегирует в application, но grep показывает, что CareerTrack.vue использует собственный RANK_LABELS напрямую, а не getEducationRequirementLabel из application. Фасадный метод в game-store мёртвый.

6. canStartEducationProgramWithReason — пробрасывает _programId, но не использует

Параметр _programId никак не влияет на результат — проверка зависит только от isEmployed и hasActiveProgram. Это вводит в заблуждение: вызывающий код (ProgramList.vue) передаёт program.id, ожидая program-specific проверку, но получает общий ответ.


План A: Зачистка zombie-кода в stores

Решить судьбу actions-store: подключить trackExecution к useActions или удалить store
Удалить FinanceSnapshot из game-store types (orphan)
Удалить getStats (нет потребителей)
Удалить getEducationRequirementLabel фасад (нет потребителей)
План B: Дедупликация типов между application и stores

CareerTrackEntry: сделать один canonical тип в application/game/index.types.ts, в game-store импортировать его
Аудит всех типов в stores/game-store/index.types.ts — проверить, какие из них реально нужны для внешних потребителей, а какие дублируют application-типы
План C: Усиление архитектурных тестов

Починить мёртвый тест stores do not use Pinia internals in other layers (пустое тело цикла)
Починить мёртвый тест application exports from index.ts only (always-true assertion)
Добавить тест: «actions-store не содержит execution logic»
Добавить тест: «pages не вызывают game-store.canExecuteAction / executeAction»
План D: Семантика canStartEducationProgram

Либо передавать program-specific правила в application query (минимальный возраст, требования к навыкам)
Либо убрать параметр _programId из сигнатуры, если проверка всегда одинакова