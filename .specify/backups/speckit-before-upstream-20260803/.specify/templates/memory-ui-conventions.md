# UI Conventions (durable UI memory)

<!--
  Durable UI-modeling conventions, reusable components and design-token mappings that
  outlive a single feature. Updated by /speckit.finalize (the UI memory sync step) from
  the work-item's ui-plan.md. Keep it compact and project-specific; do not restate the
  ui-plan template. One-off facts (a feature's concrete ST/IX/test matrix) stay in
  specs/NNN/ui-plan.md and are NOT brought here.
-->

## State-modeling conventions

<!-- e.g. forms = FormState; pagination = flags inside the `loaded` variant;
     composite screens decomposed into regions; empty modeled as … -->

- TODO: capture conventions as they emerge from finalize.

## Reusable components / patterns

<!-- Components extracted into a shared layer and reused across screens. -->

- TODO:

## Design-token / theme conventions

<!-- How mockup tokens map to theme entries; theming conventions (light/dark, scale). -->

- TODO:
