import type { TimeState } from './index.types'

export const INITIAL_STATE: TimeState = {
  totalHours: 0,
  gameDays: 0,
  gameWeeks: 0,
  gameMonths: 0,
  gameYears: 0,
  currentAge: 18,
  sleepDebt: 0,
}

export const START_AGE = 18
